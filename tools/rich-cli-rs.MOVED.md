# rich-cli-rs moved

`rich-cli-rs` is now a standalone repository:

`/home/msaraiva/dev/mark/Proj/Libs/rich-cli-rs`

This keeps `rich-rs` focused on the library + demo, while `rich-cli-rs` acts as a real-world CLI stress test with its own roadmap.

